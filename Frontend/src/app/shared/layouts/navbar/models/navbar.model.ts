/**
 * Stato del componente Navbar.
 * Gestisce la visibilità del menu mobile e altri stati di visualizzazione della barra di navigazione.
 */
export interface NavbarState {
  /** Flag che indica se il menu mobile (hamburger) è aperto. */
  menuOpen: boolean;
}
